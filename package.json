{
  "name": "tracekeep",
  "version": "0.3.0",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @tracekeep/tracekeepd dev",
    "dev:web": "pnpm --filter @tracekeep/web dev",
    "start": "pnpm --filter @tracekeep/tracekeepd start",
    "test": "pnpm -r test && node --test scripts/competition/*.test.mjs plugins/tracekeep/hooks/*.test.mjs",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
