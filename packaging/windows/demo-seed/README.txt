Atlas Build Week demo seed

The first launch copies this directory into work\demo-data before Atlas starts.
No SQLite database or personal content is shipped in source control. Atlas imports
the approved synthetic JSON examples through its local HTTP API after startup.
